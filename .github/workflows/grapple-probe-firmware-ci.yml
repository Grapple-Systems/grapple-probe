name: Grapple Probe Firmware (CI)

on:
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  build-test:
    uses: ./.github/workflows/reusable/grapple-probe-firmware-build-test.yml